# trpc-agent-go 大模型调用状态对象分析

## 一、调用大模型时的状态对象

项目在调用大模型时涉及 **5 层状态对象**，从外到内依次为：

### 1. Graph State（图状态）

- **定义位置**: `graph/state.go:68` — `type State map[string]any`
- **作用**: 图执行过程中节点间传递的共享数据结构
- **预定义键**:

| 键名常量 | 值 | 作用 |
|---|---|---|
| `StateKeyUserInput` | `"user_input"` | 用户输入，LLM执行后消费清除 |
| `StateKeyOneShotMessages` | `"one_shot_messages"` | 一次性消息，消费后清除 |
| `StateKeyOneShotMessagesByNode` | `"one_shot_messages_by_node"` | 按节点ID分组的一次性消息 |
| `StateKeyLastResponse` | `"last_response"` | 最后一次LLM响应 |
| `StateKeyLastToolResponse` | `"last_tool_response"` | 最后一次工具输出JSON |
| `StateKeyNodeResponses` | `"node_responses"` | 节点响应集合 |
| `StateKeySession` | `"session"` | 会话对象 |
| `StateKeyMessages` | `"messages"` | 消息列表（LLM节点使用和更新） |
| `StateKeyLastResponseID` | `"last_response_id"` | 最后模型响应ID |
| `StateKeyMetadata` | `"metadata"` | 元数据 |
| `StateKeyExecContext` | `"exec_context"` | 执行上下文 |
| `StateKeyToolCallbacks` | `"tool_callbacks"` | 工具回调 |
| `StateKeyModelCallbacks` | `"model_callbacks"` | 模型回调 |
| `StateKeyAgentCallbacks` | `"agent_callbacks"` | Agent回调 |
| `StateKeyCurrentNodeID` | `"current_node_id"` | 当前节点ID |
| `StateKeyParentAgent` | `"parent_agent"` | 父GraphAgent |

### 2. Invocation（调用上下文）

- **定义位置**: `agent/invocation.go:92` — `type Invocation struct`
- **作用**: 一次流执行的完整上下文，贯穿整个Agent调用生命周期
- **核心字段**:

| 字段 | 类型 | 作用 |
|---|---|---|
| `Agent` | `Agent` | 被调用的Agent实例 |
| `AgentName` | `string` | Agent名称 |
| `InvocationID` | `string` | 调用唯一ID |
| `Session` | `*session.Session` | 会话对象 |
| `Model` | `model.Model` | 使用的模型 |
| `Message` | `model.Message` | 发送给Agent的消息 |
| `RunOptions` | `RunOptions` | 运行时选项 |
| `TransferInfo` | `*TransferInfo` | Agent转移信息 |
| `StructuredOutput` | `*model.StructuredOutput` | 结构化输出配置 |
| `MemoryService` | `memory.Service` | 记忆服务 |
| `ArtifactService` | `artifact.Service` | 制品服务 |
| `state` | `map[string]any` | 调用级别的临时状态（懒初始化） |
| `MaxLLMCalls` | `int` | LLM调用次数上限 |
| `MaxToolIterations` | `int` | 工具迭代次数上限 |

- **内部状态键**（Invocation.state 中的特殊键）:

| 键名 | 常量 | 作用 |
|---|---|---|
| `"__flush_session__"` | `flusherStateKey` | flush附件标记 |
| `"__graph_barrier__"` | `barrierStateKey` | 内部屏障标记 |
| `"__append_event__"` | `appenderStateKey` | 事件追加附件 |
| `"__live_session__"` | `liveSessionStateKey` | 实时会话指针 |
| `"__graph_stream_hub__"` | `streamHubStateKey` | 图内流共享 |
| `"__trpc_agent_internal_surface_root_node_id_state__"` | `surfaceRootNodeIDStateKey` | Surface根节点ID |
| `"__trpc_agent_internal_team_member_trace_root_state__"` | `teamMemberTraceRootStateKey` | Team成员追踪根 |
| `"__sync_summary_intra_run__"` | `SyncSummaryIntraRunStateKey` | 同步摘要标记 |

### 3. RunOptions（运行选项）

- **定义位置**: `agent/invocation.go:1014` — `type RunOptions struct`
- **作用**: 单次Run的配置参数，可覆盖Agent级别默认值
- **关键字段**:

| 字段 | 类型 | 作用 |
|---|---|---|
| `AppName` | `string` | 覆盖Runner默认应用名 |
| `RuntimeState` | `map[string]any` | 运行时动态参数（如room_id, user_id） |
| `EventFilterKey` | `string` | 事件过滤键 |
| `KnowledgeFilter` | `map[string]any` | 知识检索元数据过滤 |
| `Messages` | `[]model.Message` | 完整对话历史 |
| `InjectedContextMessages` | `[]model.Message` | 注入但不持久化的上下文消息 |
| `Model` | `model.Model` | 覆盖Agent默认模型 |
| `ModelName` | `string` | 按名称选择模型 |
| `ModelSelector` | `ModelSelector` | 每次LLM调用前的模型选择器 |
| `Instruction` | `string` | 覆盖Agent指令 |
| `GlobalInstruction` | `string` | 覆盖Agent全局指令（系统提示） |
| `ToolFilter` | `tool.FilterFunc` | 工具过滤函数 |
| `AdditionalTools` | `[]tool.Tool` | 仅本次Run可见的额外工具 |
| `ExternalTools` | `[]tool.Tool` | 调用方执行的外部工具 |
| `ToolPermissionPolicy` | `tool.PermissionPolicy` | 工具权限策略 |
| `StructuredOutput` | `*model.StructuredOutput` | 结构化输出配置 |

### 4. Session State（会话状态）

- **定义位置**: `session/session.go:57` — `Session.State StateMap`，其中 `StateMap = map[string][]byte`
- **作用**: 跨请求持久化的会话级状态
- **状态前缀体系**（`session/state.go:14-17`）:

| 前缀 | 常量 | 作用 | 持久化范围 |
|---|---|---|---|
| `"app:"` | `StateAppPrefix` | 应用级状态 | 同一AppName下所有用户/会话共享 |
| `"user:"` | `StateUserPrefix` | 用户级状态 | 同一用户下所有会话共享 |
| `"temp:"` | `StateTempPrefix` | 临时状态 | 仅当前会话，不跨会话持久化 |

### 5. CallbackContext / ToolContext（回调/工具上下文）

- **定义位置**: `agent/callbackcontext.go:24` / `agent/toolcontext.go:17`
- **作用**: 工具调用和回调中使用的上下文包装
- **核心字段**:

| 字段 | 类型 | 作用 |
|---|---|---|
| `Context` | `context.Context` | 标准上下文（携带Invocation） |
| `invocation` | `*Invocation` | 关联的调用实例 |
| `State` | `session.StateMap` | 当前会话状态的Delta视图 |

---

## 二、变量遮盖（Shadowing）关系

从**高优先级到低优先级**的遮盖链：

```
RunOptions 覆盖 → Agent Options(LLMAgent) 覆盖 → Session State → Graph State
```

具体遮盖规则：

### 1. 指令/系统提示遮盖链

```
Surface Patch（表面补丁） → RunOptions.Instruction/GlobalInstruction → ModelInstructions/ModelGlobalInstructions → Agent Options.Instruction/GlobalInstruction
```

- 代码位置: `agent/llmagent/llm_agent.go:2217-2269`
- `instructionForInvocation()`: 先检查Surface Patch → 再检查RunOptions → 再检查ModelInstructions → 最后用Agent默认
- `systemPromptForInvocation()`: 同上逻辑

### 2. 模型选择遮盖链

```
RunOptions.Model → RunOptions.ModelName → RunOptions.ModelSelector → Agent.ModelSelector → Agent.Model
```

### 3. 工具表面遮盖链

```
RunOptions.ToolFilter（过滤可见工具） → RunOptions.ToolExecutionFilter（过滤可执行工具） → RunOptions.ToolPermissionPolicy（权限检查） → Agent.Tools
```

- `ToolFilter` 控制模型可见性
- `ToolExecutionFilter` 控制框架是否自动执行
- `ToolPermissionPolicy` 执行前权限检查

### 4. 状态数据遮盖链

```
Session.State.Delta → Session.State.Value → Invocation.state → RunOptions.RuntimeState
```

- Session的 `Get()` 方法: Delta优先于Value（`session/state.go:44`）

### 5. Prompt模板变量遮盖

```
{invocation:key} → 从Invocation.state解析
{app:key}/{user:key}/{temp:key} → 从Session.State解析
{name} → 先查Invocation.state，再查Session.State
```

- 代码位置: `internal/prompt/adapter/state/render.go:114-136`

---

## 三、各状态对象的占位符变量

### Prompt模板占位符（`internal/prompt/adapter/state/render.go:43-56`）

| 占位符语法 | 示例 | 作用 | 解析来源 |
|---|---|---|---|
| `{name}` / `{{name}}` | `{capital_city}` | 裸标识符替换 | Invocation.state → Session.State |
| `{name?}` / `{{name?}}` | `{capital_city?}` | 可选标识符（未解析时折叠为空串） | 同上 |
| `{app:key}` | `{app:global_config}` | 应用级状态 | Session.State（app:前缀） |
| `{user:key}` | `{user:pref}` | 用户级状态 | Session.State（user:前缀） |
| `{temp:key}` | `{temp:session_data}` | 临时状态 | Session.State（temp:前缀） |
| `{invocation:key}` | `{invocation:start_time}` | 调用级状态 | Invocation.state |
| `{artifact.filename}` / `{artifact.filename?}` | `{artifact.report.pdf?}` | 制品引用 | 特殊解析 |

---

## 四、各变量添加到状态对象的条件

### Graph State 变量添加条件

| 变量 | 添加条件 |
|---|---|
| `user_input` | 用户发起请求时设置，LLM成功执行后清除 |
| `one_shot_messages` | 需要完全覆盖当前轮次输入时设置，消费后清除 |
| `one_shot_messages_by_node` | 并行分支为不同LLM节点准备输入时设置 |
| `last_response` | LLM节点成功执行后设置 |
| `last_tool_response` | Tools节点成功执行工具后设置 |
| `messages` | LLM节点使用和更新 |
| `current_node_id` | 执行器进入某节点时设置 |

### Session State 变量添加条件

| 变量前缀 | 添加条件 |
|---|---|
| `app:*` | 通过 `UpdateAppState()` 显式设置，跨用户持久化 |
| `user:*` | 通过 `UpdateUserState()` 显式设置，跨会话持久化 |
| `temp:skill:loaded:*` | `skill_load` 工具被调用时自动添加 |
| `temp:skill:docs:*` | `skill_load` 工具选择文档时添加 |
| `temp:skill:loaded_order:*` | `skill_load` 调用时追踪加载顺序 |
| `temp:skill:artifacts` | `skill_run`/`skill_exec` 产生制品引用时添加 |
| `temp:__token_threshold_*` | 摘要检查器用于追踪token阈值 |

### Invocation.state 变量添加条件

| 变量 | 添加条件 |
|---|---|
| `__flush_session__` | flush.Attach()调用时 |
| `__graph_barrier__` | 图内部屏障机制 |
| `__append_event__` | 事件追加附件 |
| `__live_session__` | 子Agent需要恢复Runner的live session时 |
| `__graph_stream_hub__` | 图执行开始时创建 |
| `agent:*` / `model:*` / `tool:*` / `middleware:*` / `custom:*` | 回调/中间件/自定义逻辑通过 `SetState()` 设置 |

### RunOptions.RuntimeState 添加条件

| 条件 | 说明 |
|---|---|
| 调用 `WithRuntimeState(state)` | Runner.Run()时传入 |
| 调用 `MergeRuntimeState(state)` | 合并到已有RuntimeState |

---

## 五、执行技能/工具/MCP工具过程中的相关变量

### Skill 执行相关变量

| 变量键 | 来源 | 作用 |
|---|---|---|
| `temp:skill:loaded:{skillName}` | `skill_load` StateDelta | 标记技能已加载 |
| `temp:skill:loaded_by_agent:{agentName}/{skillName}` | `skill_load` StateDeltaForInvocation | 按Agent作用域标记技能已加载 |
| `temp:skill:docs:{skillName}` | `skill_load` StateDelta | 存储文档选择 |
| `temp:skill:docs_by_agent:{agentName}/{skillName}` | `skill_load` StateDeltaForInvocation | 按Agent作用域存储文档选择 |
| `temp:skill:loaded_order:` | `skill_load` 追踪 | 技能加载顺序（用于MaxLoadedSkills淘汰） |
| `temp:skill:loaded_order_by_agent:{agentName}` | `skill_load` 追踪 | 按Agent作用域的加载顺序 |
| `temp:skill:artifacts` | `skill_run`/`skill_exec` | 制品引用（用于重放） |

### 工具执行相关变量（Context传递）

| 变量 | 来源 | 作用 |
|---|---|---|
| `ContextKeyToolCallID{}` | context.Value | 当前工具调用ID |
| `contextKeyStructuredStreamErrors{}` | context.Value | 是否期望结构化错误流 |
| `contextKeyFinalResultChunks{}` | context.Value | 是否期望最终结果块 |
| `invocationKey{}` | context.Value | 当前Invocation实例 |

### MCP 工具执行相关变量

| 变量 | 来源 | 作用 |
|---|---|---|
| `mcpToolRef` | `mcpTool` 结构体 | MCP工具定义引用 |
| `sessionManager` | `mcpTool` 结构体 | MCP会话管理器 |
| `ToolMetadata` | MCP Annotations 转换 | ReadOnly/Destructive/OpenWorld 元数据 |

### 工具回调中的状态变量

工具执行时通过 `ToolContext` → `CallbackContext` 访问：
- `State`（`session.StateMap`）: 当前会话状态的Delta视图，可读写
- `invocation`: 关联的Invocation实例
- `ArtifactService`: 制品服务（保存/加载/列出/删除制品）

### 工具执行结果的状态更新

工具执行完成后，实现了 `stateDeltaProvider` 接口的工具（如 `skill_load`, `skill_run`, `skill_exec`）会通过 `StateDelta()` 方法返回需要更新到 Session State 的增量数据，框架会自动将这些增量合并到会话状态中。
